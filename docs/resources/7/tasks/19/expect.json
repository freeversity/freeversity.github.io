{
    "assertions": [
        {
            "title": "Задайте классу `copyright` автоматический внешний отступ сверху. Копирайт прижмётся к нижней границе подвала.",
            "name": "addMarginTopAutoToCopyright",
            "expect": "expect(document).to.have.cssRule('.copyright', 'marginTop', 'auto');"
        },
        {
            "title": "В `index.html` на `6` строке удалите подключение файла `portfolio-outline.css`.",
            "name": "removePortfolioOutlineCssLink",
            "expect": "expect(document.querySelector('link[href=\"portfolio-outline.css\"]')).not.to.exist;"
        }
    ]
}

# Псевдокласс `:last-of-type`

В предыдущем задании мы рассмотрели псевдокласс `:first-of-type`.

Псевдокласс `:last-of-type` работает аналогично, только выбирает _последний_ дочерний элемент родителя с учетом типа.

Пример записи:

```css
ul:last-of-type {
  ...
}
```

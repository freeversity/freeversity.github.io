# Особенности `transform-origin` — часть 3

А теперь давайте посмотрим, как меняется поведение трансформации поворота `transform: rotate` в зависимости от разных значений `transform-origin`.

В данном случае будет изменяться расположение оси вращения элемента.